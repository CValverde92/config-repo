server:
  port: 8080

spring:
  application:
    name: customer-service
  data:
    mongodb:
      host: localhost
      port: 27017
      database: customerdb

# Configuración para comunicarse con otros servicios
services:
  account:
    url: http://localhost:8081
  credit:
    url: http://localhost:8082